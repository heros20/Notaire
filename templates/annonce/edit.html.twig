{% extends 'back.html.twig' %}

{% block title %}Edit Annonce
{% endblock %}

{% block body %}
	<div class="shadow-sm p-3 mb-5 bg-white rounded form">
		<h1 class="ml-5 mb-3">Edit Annonce</h1>
		<div class="flex">
			<a class="btn btn-primary ml-5" href="{{ path('annonce_index') }}">Retour</a>
			{{ include('annonce/_delete_form.html.twig') }}
		</div>

		{{ include('annonce/_form.html.twig', {'button_label': 'Modifier'}) }}

	</div>
	
{% endblock %}
