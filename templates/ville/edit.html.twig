{% extends 'back.html.twig' %}

{% block title %}Éditer une Ville
{% endblock %}

{% block body %}
	<div class="shadow-sm p-3 mb-5 bg-white rounded form">
		<h1>Éditer une Ville</h1>

		{{ include('ville/_form.html.twig', {'button_label': 'Modifiez'}) }}
	</div>

	<div class="flex">
		<a class="btn-primary" href="{{ path('ville_index') }}">Retour</a>
		{{ include('ville/_delete_form.html.twig') }}
	</div>

{% endblock %}
