{% extends 'back.html.twig' %}

{% block title %}Editer la Catégorie
{% endblock %}

{% block body %}
	<div class="shadow-sm p-3 mb-5 bg-white rounded form">

		<h1 class="ml-5">Editer la Catégorie</h1>

		{{ include('category/_form.html.twig', {'button_label': 'Modifiez'}) }}
	</div>

	<div class="flex">
		<a class="btn btn-primary ml-5" href="{{ path('category_index') }}">Retour</a>
		{{ include('category/_delete_form.html.twig') }}
	</div>
{% endblock %}
